lex_analyzer is the lexical analyzer program that will also show errors


lex_analyzer_code.py is the code behind the lex_analyzer

copy the test file code(s) into the scanner of the lex_analyzer.py program for lexemes list
